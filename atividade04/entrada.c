#include <stdio.h>

int main(int argc, char *argv[]) {
  
  int a, b, c;
  scanf("%d %d %d", &a, &b, &c);
  printf("Você digitou: %d %d %d\n", a, b, c);
  long l = 10;
  int a = l


  float a = 1.0;
  float b = 1.0;
  double sqrt = a * b + b * a;
  printf ("Número real: %.2f", sqrt);

  return 0;
} 
